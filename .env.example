# DockPilot Environment Configuration

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:48391
BACKEND_URL=http://backend:8000

# Docker Configuration
DOCKER_HOST=unix:///var/run/docker.sock

# Search Paths for Docker Compose Apps (comma-separated)
# These will be mounted in the backend container
SEARCH_PATHS=${HOME}/docker,${HOME}/Docker,/opt/apps,/opt/docker

# Resource Monitoring
RESOURCE_POLL_INTERVAL=2

# Log Settings
LOG_RETENTION_DAYS=7
MAX_LOG_LINES=1000

# CORS Settings (comma-separated origins)
CORS_ORIGINS=http://localhost:38572,http://127.0.0.1:38572
